---
branch: roadmap/zero-trust-v2-v4-hardening
status: complete
last_updated: 2026-04-17T18:14:07-04:00
workstream: zero-trust roadmap execution handoff
---

<current_state>
Repo has no `.planning/` workspace, so this root handoff file remains the resume anchor.

Known good implementation state now:
- `543ed8b` already landed V2 Loop 1 and V2 Loop 2.
- Later work completed V2 Loop 3 through V2 Loop 6.
- Later work completed V3 Loop 0 through V3 Loop 4.
- This session completed V4 Loop 0 through V4 Loop 5.
- The current roadmap defined in this repo ends at `V4 Loop 5`.
- The local tree now contains the completed V2, V3, and V4 roadmap work, all still uncommitted.
</current_state>

<completed_work>

- Implemented V4 Loop 0 policy engine maturity:
  - explainable policy output over action, target, actor, environment, and risk
  - explicit `allow`, `require_approval`, `step_up`, and `deny` behavior
  - policy summaries carried into request and approval payloads
- Implemented V4 Loop 1 attestation and runtime identity:
  - added `src/identity.rs`
  - added stub signed-header verification for runtime, host, adapter, timestamp, and signature claims
  - bound verified identity into request, approval, execute, and health surfaces
  - fail-closed on missing, invalid, stale, or mismatched identity
- Implemented V4 Loop 2 tamper-evident operations and forensics:
  - extended `src/audit.rs` with audit-chain verification and redact-safe forensic export
  - added `src/bin/forensics.rs`
- Implemented V4 Loop 3 rotation and recovery drills:
  - added `scripts/run-rotation-recovery-drills.sh`
  - added approver-rotation regression coverage
- Implemented V4 Loop 4 adversarial continuous verification:
  - added `scripts/run-adversarial-suite.sh`
  - added PR-safe and extended adversarial lanes
  - wired scheduled extended adversarial CI
- Implemented V4 Loop 5 platform ship gate:
  - added `docs/PLATFORM_SUPPORT.md`
  - extended `docs/RELEASE.md` with V4 ship gate, claims table, and signoff
  - added `scripts/check-v4-ship-gate.sh`
  - updated `.github/workflows/ci.yml` with rotation drills, PR-safe adversarial suite, V4 gate summary, and scheduled extended adversarial job

Full verification passed:
- `cargo fmt --all -- --check`
- `cargo check --all-targets --all-features`
- `cargo test --all-targets --all-features -- --nocapture`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `bash scripts/run-e2e-harness.sh`
- `bash scripts/check-v2-ship-gate.sh`
- `bash scripts/check-v3-ship-gate.sh`
- `bash scripts/check-v4-ship-gate.sh`
- `bash scripts/run-rotation-recovery-drills.sh`
- `bash scripts/run-adversarial-suite.sh pr`
- `bash scripts/run-adversarial-suite.sh extended`
</completed_work>

<remaining_work>

- No further loop is defined in the current roadmap.
- Any future work is post-roadmap scope and should start from the current V2/V3/V4 gates instead of reopening completed loops.
</remaining_work>

<decisions_made>

- Treat V4 completion as repo-owned completion with matrix-bound external claims.
- Treat `docs/RELEASE.md` and `docs/PLATFORM_SUPPORT.md` as the V4 ship authority.
- Keep external runtimes matrix-bound unless future evidence upgrades them.
</decisions_made>

<blockers>
- No technical blocker in current repo state.
- Remaining constraint is only truthfulness: do not overstate external-host support beyond the current matrices.
</blockers>

<context>
Current support truth is still intentionally narrow:

- `docs/SUPPORTED_HOSTS.md` governs V3 host claims.
- `docs/PLATFORM_SUPPORT.md` governs V4 platform and control claims.
- OpenClaw remains preview.
- Claude/Codex/arbitrary external runtimes remain unsupported for shipped trust claims.
- Identity verification is a stub local attestation contract, not hardware-backed or vendor-specific attestation.
- Trusted execution adapters remain sanctioned stub helper-path contracts, not production browser automation or provider mediation.
</context>

<next_action>
If new work starts:

1. review `docs/RELEASE.md`
2. review `docs/PLATFORM_SUPPORT.md`
3. keep `bash scripts/check-v2-ship-gate.sh`, `bash scripts/check-v3-ship-gate.sh`, and `bash scripts/check-v4-ship-gate.sh` green
4. treat new work as post-V4 scope, not as another unfinished loop
</next_action>
