---
branch: roadmap/zero-trust-v2-v4-hardening
status: v5_planning_ready
last_updated: 2026-04-18T08:15:00-04:00
workstream: post-v4 roadmap and docs IA handoff
---

<current_state>
Repo has no `.planning/` workspace, so this root handoff file remains the resume anchor.

Known good state now:
- `543ed8b` already landed V2 Loop 1 and V2 Loop 2.
- Later work completed V2 Loop 3 through V2 Loop 6.
- Later work completed V3 Loop 0 through V3 Loop 4.
- Later work completed V4 Loop 0 through V4 Loop 5.
- Later work completed the post-V4 production bridge tasks.
- This session seeded the canonical `V5` through `V10` roadmap in `ROADMAP.md`.
- This session completed the docs information architecture cleanup.
- The branch is clean and the next real execution step is `V5 Loop 0`.
</current_state>

<completed_work>

- Completed the old roadmap foundation:
  - `V2` shipped
  - `V3` shipped with matrix-bound external claims
  - `V4` shipped
- Added the canonical product ladder in `ROADMAP.md`:
  - `V5` Multi-Host Ship
  - `V6` Production Action Surface
  - `V7` Enterprise Trust Platform
  - `V8` Enterprise Depth
  - `V9` Platform Breadth
  - `V10` Category Platform
- Completed the docs IA cleanup:
  - added `docs/INDEX.md`
  - added `docs/backlog/INDEX.md`
  - moved canonical docs into `docs/product/`, `docs/architecture/`, and `docs/operations/`
  - moved plans into version-owned folders under `docs/plans/`
  - moved specs into `docs/specs/`
  - seeded version-owned backlog folders under `docs/backlog/`
  - removed legacy `docs/superpowers/` planning directories
  - repaired migrated doc links and verified the tree

Docs verification passed:
- `find docs -maxdepth 3 -type f | sort`
- `find docs/superpowers -maxdepth 3 -type f 2>/dev/null || true`
- `git status --short --branch`
</completed_work>

<remaining_work>

- `V5` is planned, not implemented.
- `V5 Loop 0` Host Contract Canon is the next real step.
- No `V5` execution plan exists yet for host-contract implementation work.
</remaining_work>

<decisions_made>

- Treat `ROADMAP.md` as the single roadmap source of truth.
- Treat `docs/INDEX.md` as the human docs entrypoint, not a second roadmap.
- Keep first-class host support matrix-bound until each host earns shipped evidence.
- Start `V5` with Host Contract Canon before any per-host certification work.
</decisions_made>

<blockers>
- No canonical `V5` host contract exists yet.
- Hermes, Claude, and Codex do not yet have host-specific certification lanes.
</blockers>

<context>
Current support truth is still intentionally narrow:

- `docs/product/SUPPORTED_HOSTS.md` governs host claims.
- `docs/product/PLATFORM_SUPPORT.md` governs platform and control claims.
- OpenClaw remains preview.
- Claude/Codex/arbitrary external runtimes remain unsupported for shipped trust claims.
- The roadmap now continues beyond V4; do not use the old “roadmap ended at V4” assumption.
</context>

<next_action>
If new work starts:

1. review `ROADMAP.md`
2. review `docs/INDEX.md`
3. review `docs/product/RELEASE.md`
4. review `docs/product/SUPPORTED_HOSTS.md`
5. write the `V5 Loop 0` host contract canon spec and execution plan before implementation
</next_action>
